# Nova CI‑Rescue configuration (.env)
# Copy this file to .env and fill in values as needed.
# Note: Secrets are never logged by Nova telemetry. Keep this file private.

# LLM providers (optional but recommended)
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenSWE external coding service (optional)
OPENSWE_BASE_URL=
OPENSWE_API_KEY=

# Runtime knobs (optional)
# Maximum agent iterations before stopping (default: 6)
NOVA_MAX_ITERS=6
# Overall run timeout in seconds (default: 1200 = 20 minutes)
NOVA_RUN_TIMEOUT_SEC=1200
# Pytest timeout in seconds (default: 600 = 10 minutes)
NOVA_TEST_TIMEOUT_SEC=600

# Advanced (optional)
# Comma-separated domain allow-list for outbound HTTP (overrides defaults)
# NOVA_ALLOWED_DOMAINS=api.openai.com,api.anthropic.com,pypi.org,files.pythonhosted.org,github.com,raw.githubusercontent.com
# Telemetry output directory (default: telemetry)
# NOVA_TELEMETRY_DIR=telemetry
# Default LLM model (default: gpt-4o-mini)
# NOVA_DEFAULT_LLM_MODEL=gpt-4o-mini
